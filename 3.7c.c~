#include <stdio.h>

int main(void)
{
	long int n = 2;
	
	for (long int i = 113234; n > 0 ; i++)
	{
		n = i;
		while (n != 1)
		{
			printf("%d ", n);
			if (n % 2 == 0)
				n /= 2;
			else n = n * 3 + 1;
			
		}
		printf("i : %d\n", i);
	}
	printf("\n");

	return 0;
}

