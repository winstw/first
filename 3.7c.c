#include <stdio.h>

int main(void)
{
	int nombre;
	printf("entre un nombre : ");
	scanf("%d, &nombre);
	while (nombre != -1)
	{
	 

		while ( !=1)
		{
			printf("%d ", n);
			if (n % 2 == 0)
				n /= 2;
			else n = n * 3 + 1;
		
		}
	
		
	}
	printf("\n");

	return 0;
}

